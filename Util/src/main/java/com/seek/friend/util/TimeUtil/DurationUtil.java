package com.seek.friend.util.TimeUtil;

import java.time.Duration;

public class DurationUtil {
    public static Duration getSecondDuration(long duration) {
        return Duration.ofSeconds(duration);
    }
}
