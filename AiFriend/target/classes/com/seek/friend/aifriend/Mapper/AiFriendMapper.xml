<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seek.friend.aifriend.Mapper.AiFriendMapper">

    <!-- 全局统一字段映射 -->
    <resultMap id="AiFriendResultMap" type="com.seek.friend.serviceobject.AiFriend.AiFriendDTO">
        <result property="complete" column="is_complete" />
        <result property="delete" column="is_delete" />
    </resultMap>


    <insert id="init">
        insert into "ai_friend"
        (ai_friend_id,
        user_id,
        name)
        values(
        #{aiFriendId},
        #{userId},
        #{name})
    </insert>

    <insert id="updateText">
        update "ai_friend"
        <set>
            <if test="description!=null">description=#{description},</if>
            <if test="hobbies!=null">hobbies=#{hobbies},</if>
            <if test="characteristic!=null">characteristic=#{characteristic},</if>
            <if test="encounterReason!=null">encounter_reason=#{encounterReason},</if>
            <if test="likeScore!=null">like_score=#{likeScore}</if>
        </set>
    </insert>

    <select id="updateHeader" resultType="java.lang.String">
        update "ai_friend"
        set header_image_addr=#{addr}
        where ai_friend_id=#{aiFriendId}
        and user_id=#{userId} and is_delete=false and is_complete=false
<!--    pg18的语法-->
        returning old.header_image_addr
    </select>

    <select id="simpleGetList" resultMap="AiFriendResultMap">
        select
        ai_friend_id,
        user_id,
        name,
        header_image_addr,
        create_time,
        is_complete
        from "ai_friend"
        where user_id=#{userId} and is_delete=false
        <if test="complete!=null">and is_complete=#{complete}</if>
        order by ai_friend_id desc
        limit #{need} offset #{start}
    </select>


    <select id="getDetail" resultMap="AiFriendResultMap">
        select
        ai_friend_id,
        user_id,
        name,
        description,
        hobbies,
        characteristic,
        encounter_reason,
        like_score,
        header_image_addr,
        create_time,
        is_complete,
        is_delete
        from "ai_friend" where ai_friend_id=#{aiFriendId} and user_id=#{userId} and is_delete=false
    </select>

    <select id="delete" resultType="java.lang.Boolean">
        update "ai_friend"
        set is_delete=true
        where ai_friend_id=#{aiFriendId} and user_id=#{userId} and is_delete=false
        returning is_complete
    </select>







</mapper>